let GameMap = require('./gamemap');
let Team = require('./team');

/*
  Prototype NPC:
    Main idea is to have several different types of actions, each of them has a actions associated and a action value
    An action is an in-game option the npc can take
    An action value is the maximun calculated 'profit' (still subjective definition) of every action
*/

let NPC = function() {

  this.mainOperation = function() {

  }

  let colonization = () => {
    this.actionValue = () => {

    }
    this.action = () => {

    }
  }

}

module.exports = NPC;